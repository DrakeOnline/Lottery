
#include <vector>

namespace DrakeOnline {

    class Algorithm {
        public:
            int* decrement ();
            int* knuthShuffle ();
            std::vector<int> subVector (void);
            int generateRandomNumber(int, int);
    };

}
